let options = {
    strings: ['kerakli ma`lumotni topshish uchun', ' so`z kiriting'],
    typeSpeed: 40,
    startDelay: 1000,
    loop: true,
    loopCount: Infinity,
};

let typed = new Typed('.search_input__center', options);